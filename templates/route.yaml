{{- range $service, $config := .Values.services }}
{{- if and (eq $.Values.global.network.expose.type "route") ( $config.enabled ) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $service }}
  namespace: {{ $.Release.Namespace }}
spec:
  parentRefs:
    - name: {{ $.Values.global.network.expose.gateway.name }}
      namespace: {{ $.Values.global.network.expose.gateway.namespace }}
      sectionName: https
      group: gateway.networking.k8s.io	
      kind: Gateway
  hostnames:
    - {{ $service }}.{{ $.Values.global.network.domain }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ $service }}
          namespace: {{ $.Release.Namespace }}
          port: 80
          weight: 1
          group: ''	
          kind: Service
{{- end }}
{{- end }}